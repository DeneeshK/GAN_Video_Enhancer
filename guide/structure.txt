🧩 FINAL MODULE SPECIFICATIONS (UPGRADED)

Minimal, clean, permanent.

🔹 MODULE 01 — Decode + Normalize

Input: Raw compressed video
Output: Normalized video

Purpose:
Standardize format, colorspace, FPS, pixel format.

Upgrade:
Better FPS normalization + color space fixing.

🔹 MODULE 02 — Deinterlacing (BWDIF)

Input: Normalized video
Output: Progressive video

Purpose:
Remove interlacing artifacts.

Upgrade:
Higher-quality interpolation.

🔹 MODULE 03 — Stabilization (VidStab)

Input: Deinterlaced video
Output: Stabilized video

Purpose:
Remove camera shake + VHS jitter.

Upgrade:
More stable motion smoothing.

🔹 MODULE 04 — Temporal Flicker Reduction (NEW)

Input: Stabilized video
Output: Flicker-stable video

Purpose:
Remove brightness + exposure flicker.

Why:
GANs amplify flicker badly.

🔹 MODULE 05 — Edge-Preserving Pre-Sharpen (NEW)

Input: Flicker-stable video
Output: Sharpened video

Purpose:
Use OpenCV classical sharpening before ML.

Tech:
Unsharp mask + bilateral sharpening.

Why:
Natural edges before GAN = better textures.

🔹 MODULE 06 — Frame Extraction

Input: Pre-sharpened video
Output: PNG frames

Purpose:
Lossless frame extraction.

🔹 MODULE 07 — FastDVDNet Denoising

Input: Raw frames
Output: Clean frames

Upgrade:
Lower denoise strength → preserve micro textures.

🔹 MODULE 08 — Super Resolution + Texture Recovery (MODEL CHANGE)

Input: Clean frames
Output: High-resolution frames

Pipeline:

FastDVDNet → SwinIR → RealESRGAN


Why this is superior:

SwinIR preserves structure

ESRGAN adds sharpness

Zero hallucination

🔥 Huge improvement.

🔹 MODULE 09 — Detail Refinement (NEW - OpenCV)

Input: Super-res frames
Output: Refined frames

Purpose:
Controlled sharpening + micro-detail recovery.

🔹 MODULE 10 — Face Enhancement (Controlled GFPGAN)

Input: Refined frames
Output: Face-enhanced frames

Upgrade:
Selective face-only processing, low strength.

🔹 MODULE 11 — Temporal Consistency Refinement (NEW)

Input: Enhanced frames
Output: Stable frames

Purpose:
Remove shimmer, flicker, crawling textures.

🔹 MODULE 12 — Final Color Grading + Film Look

Input: Stable frames
Output: Final cinematic video

Techniques:

Color balance

Film contrast curves

Soft tone mapping

🧠 WHY OPENCV SHARPENING IS IMPORTANT

GAN sharpness = hallucinated detail
OpenCV sharpness = real edge enhancement

So we:

OpenCV → GAN → OpenCV


This sandwich gives:

Realistic clarity instead of artificial crispness.

🎯 FINAL QUALITY TARGET
Metric	Target
Texture	Natural
Faces	Organic
Edges	Sharp, not halo
Motion	Stable
Color	Cinematic
Noise	Minimal
📄 ABOUT PDF GENERATION

When we finish all modules, I can generate a:

Complete Pipeline Specification PDF

Containing:

Architecture

Module designs

Processing flow

Visual examples

CLI usage

So you’ll have a professional documentation pack.

🧠 SUMMARY OF MAJOR UPGRADES
Area	Upgrade
Super Resolution	SwinIR + ESRGAN hybrid
Pre-sharpen	OpenCV edge enhancement
Post-sharpen	Controlled micro-detail boost
Temporal	Full consistency refinement
Faces	Controlled GFPGAN